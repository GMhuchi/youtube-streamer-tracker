name: 채널 삭제 요청 처리

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: write
  issues: write

jobs:
  remove-channel:
    if: contains(github.event.issue.labels.*.name, 'remove-channel')
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 의존성 설치
        run: pip install -r requirements.txt

      - name: 이슈 본문 저장
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: printf '%s' "$ISSUE_BODY" > /tmp/issue_body.txt

      - name: 채널 삭제
        id: remove
        run: |
          python scripts/remove_channel.py \
            --issue-body-file /tmp/issue_body.txt \
            --github-output "$GITHUB_OUTPUT"

      - name: 변경사항 커밋
        if: steps.remove.outputs.success == 'true'
        run: |
          git config user.name "streamer-tracker-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/channels.json
          git commit -m "chore: 채널 삭제 - ${{ steps.remove.outputs.channel_name }} [skip ci]"
          git push

      - name: 삭제 완료 코멘트 및 이슈 닫기
        if: steps.remove.outputs.success == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ github.event.issue.number }}" --body "✅ ${{ steps.remove.outputs.message }}

          다음 트래킹 사이클(최대 5~10분 이내)부터 목록에서 사라집니다."
          gh issue close "${{ github.event.issue.number }}"

      - name: 실패 안내 코멘트
        if: steps.remove.outputs.success != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ github.event.issue.number }}" --body "⚠️ ${{ steps.remove.outputs.message }}

          사이트에 표시된 채널 이름을 정확히 복사해서 이 이슈 내용을 수정해주세요. 수정 후 다시 실행하려면 이슈를 닫았다가 다시 열어주세요."
